# compare-0007 — solo la scheda del run interno

Questa cartella **non contiene un confronto**: c'è solo il `backtest-summary.json` del
run interno del 2026-08-28 (`jobId 5ee64c1e…`, workspace `all-in`, 2024 intero, 2.425
trade, engine con `holding` overnight sì / overweek no, flat 20:45 UTC).

È lo stesso file, byte per byte, di `compare-008/internal/backtest-summary.json`. **Le
due gambe e l'esito del confronto stanno in [`../compare-008/esito.md`](../compare-008/esito.md)**,
che è l'analisi registrata altrove come "compare-0007".

Tenuta perché il nome compare-0007 ricorre nelle analisi successive (`compare-0012`,
`compare-0013`) come termine di paragone dello slippage sugli stop: 180 USD a stop su NQ.
