// Modelli per i Settings

export interface PiootooSetup {
  id: string;
  name: string;
  code: string;
  initialCapital: number;
  selectedSymbols: string[];
  createdAt: string;
  updatedAt: string;
}
